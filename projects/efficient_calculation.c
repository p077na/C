#include <stdio.h>
#include <math.h>

int main(void){
    int x = 10;
    printf("%d", pow(x, 2));
    printf("%d", pow(x, 4));
    printf("%d", pow(x, 6));
    printf("%d", pow(x, 8));

    return 0;
}